<form method="post" action="{{ path('app_formatteur_delete', {'id': formatteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formatteur.id) }}">
    <button class="btn">Delete</button>
</form>
